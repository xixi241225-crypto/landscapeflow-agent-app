# LandscapeFlow AI 2.0｜现场路演模式交付报告

## 1. 当前源码结构判断

- 技术栈：React 18 + Vite 5 + React Router 6 + Framer Motion + Tailwind CSS。
- 路由：使用 `HashRouter`，普通产品保留 `/` 与 `/workbench`，新增独立 `/roadshow`。
- 普通产品状态：Blueprint、Agent 状态、确认节点和项目记录通过 `localStorage` 持久化。
- 路演状态：使用独立的 `landscapeflow_v2_roadshow_state` 键，不覆盖普通项目状态。
- 原项目目录当前不是 Git 仓库，因此本轮没有创建分支或提交，也没有覆盖 Git 中的未提交修改。

## 2. 路演模式实现方式

路演模式以独立路由和独立状态机增量实现，不改写普通工作台业务链。

五个可见页面：

1. 路演项目载入页；
2. 项目资料总览页；
3. 项目设计蓝本页；
4. 六 Agent 设计执行轨迹页；
5. 完整成果中心页。

内部状态覆盖项目载入、蓝本确认、Agent 顺序执行、执行完成、成果标签页和持久化恢复。核心结果全部来自本地配置和本地图片，不依赖实时模型、OCR、图像生成或第三方接口。

## 3. 新增文件列表

- `src/data/roadshowProject.js`
- `src/components/roadshow/RoadshowMode.jsx`
- `public/roadshow-assets/README.md`
- `ROADSHOW_MODE_REPORT.md`
- `docs/screenshots/roadshow-mode/01-路演项目载入页-1920x1080.png`
- `docs/screenshots/roadshow-mode/02-项目资料总览-1920x1080.png`
- `docs/screenshots/roadshow-mode/03-项目设计蓝本与一次确认-1920x1080.png`
- `docs/screenshots/roadshow-mode/04-六Agent自动执行轨迹-1920x1080.png`
- `docs/screenshots/roadshow-mode/05-完整成果中心与18页PPT-1920x1080.png`
- `docs/screenshots/roadshow-mode/06-成果中心-1440x900.png`

## 4. 修改文件列表

- `src/App.jsx`：增加 `/roadshow` 路由。
- `src/components/Hero.jsx`：增加“进入现场路演”入口，不删除普通产品入口。
- `src/index.css`：增加独立路演布局、状态、时间线、成果与响应式样式。
- `README.md`：增加路演访问入口、流程和快捷键。

## 5. 路演模式访问入口

开发环境：

`http://127.0.0.1:5173/landscapeflow-agent-app/#/roadshow`

也可从产品首页右上角点击“进入现场路演”。

## 6. 启动命令

```bash
npm ci
npm run dev
```

生产构建：

```bash
npm run build
```

## 7. 完整操作流程

1. 进入 `/roadshow`。
2. 点击“载入路演项目”，约 1.7 秒完成四类资料读取反馈。
3. 在资料总览点击“生成项目设计蓝本”。
4. 查看项目目标、核心约束、设计策略与关键专业判断。
5. 点击“确认设计方向”，这是路演流程唯一一次人工确认。
6. 页面自动进入执行轨迹，点击“启动方案生成”。
7. 六个 Agent 依次执行，总时长约 11.5–12 秒。
8. 点击“查看完整成果”，默认停留在 18 页 PPT 缩略图。
9. 可切换查看项目定义、总平与分析、视觉表达和 PPT。

主叙事为：“一个项目、一个设计方向、一次人工确认、一套完整成果。”

## 8. 快捷键

- `→` 或 `Space`：下一步；
- `←`：上一步；
- `R`：完全重置；
- `1`：项目载入页；
- `2`：资料总览页；
- `3`：项目设计蓝本页；
- `4`：Agent 执行轨迹页；
- `5`：成果中心页。

输入框、文本域、下拉框或可编辑区域获得焦点时，快捷键不会拦截输入。

## 9. 路演项目数据文件位置

`src/data/roadshowProject.js`

集中配置内容包括：

- 固定项目基本信息；
- 12 份项目资料及提取结果；
- Blueprint V1.0 目标、约束与策略；
- 六 Agent 固定执行结果；
- 项目定义、概念、总平、分析图、效果图、植物与材料策略；
- 18 页 PPT 标题、结论、视觉建议、图片路径；
- PPT 文件状态与正式替换路径。

## 10. 图片素材位置

当前使用：

`public/demo-images/`

所有路演核心图片都采用相对路径并在页面加载时预加载，兼容 Vite 基础路径和构建部署路径。

## 11. 需要替换的占位素材

当前仍需用真实项目素材替换：

1. 功能分区分析图；
2. 慢行动线分析图；
3. 空间结构分析图；
4. 路演案例的正式区位图与现状问题分析；
5. PPT 第 4、8、9、10 页对应的专业图形。

推荐放置目录：

`public/roadshow-assets/`

统一图片组件在 URL 缺失或加载失败时显示专业用途占位，不会折叠或造成白屏。

## 12. PPT 当前状态

- 网页已完成 18 页 16:9 PPT 缩略图总览。
- 页面明确显示 `.pptx` 文件名和目标交付格式“原生可编辑”。
- 当前仓库内没有真实 PPTX 文件，下载按钮不会伪造成功；点击后会显示真实文件待接入及本地替换路径。
- 正式 PPTX 建议放在：
  `public/roadshow-assets/松林社区公园景观方案汇报.pptx`
- 放入文件后，再设置 `deliverables.editablePpt.fileUrl` 即可启用真实下载。

## 13. 构建测试结果

修改前：

- `npm run build` 通过；
- 408 个模块；
- Vite 构建耗时约 1.27 秒。

开发完成后：

- `npm run build` 通过；
- 410 个模块；
- `dist/assets/index-DSivTNCB.css`：56.06 kB；
- `dist/assets/index-Duof8FZh.js`：442.94 kB；
- Vite 构建耗时约 1.21 秒。

## 14. 功能测试结果

已实际完成以下浏览器测试：

- 从路演入口完整跑通；
- 重置后再次进入流程；
- 数字键、左右方向键、Space 和 R 快捷键；
- 任意阶段直接进入成果中心；
- Agent 动画执行中重置；
- 载入按钮快速双击；
- 六 Agent 完成后重新运行；
- 路由刷新恢复成果中心与确认状态；
- 本地图片正常加载；
- 缺失图像显示统一专业占位；
- PPT 下载按钮显示真实能力边界；
- 普通首页与三栏工作台回归；
- 1920×1080 与 1440×900 横向溢出检查；
- 浏览器控制台 error / warning 检查。

## 15. 已完成内容

- 独立、离线、确定性路演模式；
- 固定脱敏社区项目与 12 份项目资料表达；
- 1.7 秒分步载入反馈；
- 资料总览与提取结果；
- Blueprint V1.0 专业策略页面；
- 一次设计师人工确认；
- 确认后持续显示的 Blueprint 状态栏；
- 六 Agent 自动连续执行与状态高亮；
- 完整成果中心与四类成果标签；
- 总平、分析、效果、植物和材料策略表达；
- 18 页 PPT 缩略图；
- 重置、翻页、直达成果和快捷键；
- 独立持久化、图片预加载与失败兜底；
- 桌面投影和笔记本布局。

## 16. 未完成内容

- 没有真实 PDF / CAD / Word 内容解析；
- 没有实时 LLM 或图像生成；
- 没有真实项目分析图；
- 没有真实 PPTX 文件和生成引擎；
- 没有云端接口、登录和数据库。

这些均属于本轮明确排除项或待替换正式素材。

## 17. 当前已知问题

1. 三张分析图仍为专业占位，需要替换真实项目输出。
2. PPT 下载按钮目前只提示替换位置，因为仓库没有真实 `.pptx` 文件。
3. 路演模式为桌面优先，仅保证笔记本与 16:9 投影；没有进行移动端深度适配。
4. 项目不是 Git 仓库，本轮无法按建议创建 `feature/roadshow-mode` 分支或提交记录。

## 18. 是否存在阻断性问题

不存在。核心路演流程离线可用，页面切换、重置、刷新恢复和六 Agent 自动执行均可正常运行。

## 19. 明天最应该优先检查的 5 个位置

1. 用比赛现场电脑和投影打开 `/roadshow`，确认字体大小与浏览器缩放为 100%。
2. 将三张真实分析图替换进 `public/roadshow-assets/`。
3. 放入最终 18 页 `.pptx`，设置 `editablePpt.fileUrl` 并实测下载。
4. 按 2 分钟口播完整排练一次，确认 Agent 约 12 秒执行节奏合适。
5. 将成果中心四个标签逐一打开，核对所有图片是否与最终项目口径一致。

---

## 第二轮：项目资料向导改造

### 改造范围

本轮只调整普通产品首页、工作台三栏外壳和 Agent 1 执行前的项目资料输入流程。独立 `RoadshowMode`、右侧 Blueprint 详情、四个确认节点、后续五个 Agent、成果中心与 PPT 页面均保持原机制。

### 首页入口

- 首页首屏仅保留一个主操作“开始方案设计”；
- 点击后进入 `/workbench` 并创建空白项目；
- 删除首页“进入现场路演”“新建项目”“载入路演案例”等重复入口；
- 演示案例不再作为独立运行模式，而是在资料向导每一步按需填入。

### 工作台三栏

- 左侧改为 `ProjectHistorySidebar`，读取现有 `projectStorage` 项目记录；
- 每页显示 5 个最近项目，支持上一页、下一页、当前页码和项目恢复；
- 当前项目使用简洁选中状态；无本地记录时使用独立配置中的演示历史；
- 中间只显示当前工作内容；右侧继续使用原 `BlueprintPanel`；
- 原 `HistoryPanel` 继续负责 Blueprint 版本记录和恢复，没有删除或合并职责。

### 三页项目资料向导

1. **项目基本信息**：项目名称、地点、类型、面积、设计阶段、预算条件、设计目标和核心约束；
2. **项目资料上传**：单一拖拽/点击上传区、格式说明、资料提示标签和简洁文件列表；
3. **资料整理与完整性检查**：项目摘要、已识别信息、核心资料状态和后续深化建议。

向导使用 `projectInputStep: 0 | 1 | 2` 保存到项目级状态。上一步、下一步和页面刷新均保留字段、文件和当前步骤。资料整理阶段使用约 1.14 秒的三段可见状态，并通过任务令牌和禁用按钮避免快速重复执行。

### 一键填入演示案例

- 第 1 页只填基本信息；
- 第 2 页只加入 6 份“松林社区公园”演示资料，并按文件名去重；
- 第 3 页补齐基本信息和演示资料，但不会自动提交；
- 填入后所有字段仍可人工修改；
- 演示项目统一为“松林社区公园更新设计”，不再混用多个项目口径。

### 与 Agent 1 的衔接

第 1 页仅校验并保存基本信息，第 2 页仅整理资料。只有第 3 页点击“确认资料并生成项目设计蓝本”后，才调用工作台既有 `executeAgent(1, 'professional')`。Agent 1 的结构化 Patch、Blueprint 版本创建、项目事实确认节点和后续六 Agent 流程保持原实现。

### 本轮浏览器回归

- 首页“开始方案设计”唯一入口：通过；
- 工作台左侧仅显示历史项目：通过；
- 三页向导前后切换且数据不丢失：通过；
- 演示资料连续填入两次仍为 6 份，无同名重复：通过；
- 资料整理期间按钮禁用，完成后进入第 3 页：通过；
- 第 3 页一键填入不自动提交：通过；
- 刷新后恢复第 3 页和 6 份资料：通过；
- 第 3 页确认后 Agent 1 写入 Blueprint v1 并停在“项目事实确认”：通过；
- 历史项目加载：通过；
- Blueprint 版本记录与恢复入口：通过；
- 1920×1080 工作台：无横向溢出；
- 1440×900 工作台：`scrollWidth = 1440`，无横向溢出；
- 1280×800 补充检查：`scrollWidth = 1280`，无横向溢出；
- 浏览器控制台：仅 Vite 连接与 React DevTools 提示，无 error / warning。

### 验收截图

截图保存在：

`docs/screenshots/project-input-wizard/`

- `01-首页唯一入口-1920x1080.png`
- `02-工作台项目基本信息页-1920x1080.png`
- `03-工作台项目资料上传页-1920x1080.png`
- `04-资料整理与完整性检查页-1920x1080.png`
- `05-完整工作台三栏-1920x1080.png`
- `06-工作台适配-1440x900.png`

---

## 第三轮：路演流程简化

### 四阶段路演主流程

“开始方案设计”进入的演示项目新增独立呈现状态，统一为：

1. 项目资料；
2. 设计蓝本；
3. Agent 协作；
4. 完整成果。

完整产品模式原有六 Agent 页面、四确认节点、版本管理和数据结构继续保留。路演呈现层只显示一次设计师人工确认；后续确认节点由明确标记的预缓存路演决策自动完成。

### 历史项目

左侧路演历史固定展示当前松林社区公园项目，以及湿地公园、郊野公园、街头绿地、居住区和商业街区五种不同项目。每页 5 项，第二页显示第 6 项。项目名称、地点、类型均不重复，只有实际当前项目显示“当前”。

### 就地补充资料

资料检查页缺少核心资料时，不再要求返回上一页。当前页直接提供：

- 上传补充资料；
- 载入演示补充资料。

文件继续按名称去重。满足项目任务书、甲方需求或红线底图任一条件后，页面即时切换为“核心资料已满足，可以开始整理”，并解锁唯一主按钮“开始整理项目资料”。

### 项目设计蓝本草案

Agent 1 完成后默认只展示：

- 项目目标；
- 核心约束；
- 推荐设计策略。

项目事实、资料来源、缺口、系统假设和未确认信息统一放入默认折叠的“查看整理依据与待补充信息”。路演页面不再显示逐条事实确认、逐项假设接受/否定、重复编辑表单或技术化 Blueprint 写入按钮。

唯一人工确认操作为：

`确认设计方向并开始设计`

### 六 Agent 连续执行

确认后自动进入“六个专业 Agent 正在协同完成方案”页面。六行执行轨迹只显示 Agent 名称、一句话结果和等待/执行中/已完成状态。预缓存 Mock Provider 按 560—720ms 节奏顺序执行，完整浏览器实测约 5.2 秒。

执行页面不再显示逐个 Agent 运行、停止、阶段翻页或阻塞提示。完成后只显示“查看完整成果”，并直达带 18 页可编辑 PPT 内容预览的成果中心。

### 回归结果

- 不同景观类型历史记录：通过；
- 历史项目无重复：通过；
- 第三页就地补充与即时解锁：通过；
- 精简蓝本草案和默认折叠依据：通过；
- 路演唯一人工确认：通过；
- 六 Agent 自动连续执行：通过；
- 完整成果及 PPT 入口：通过；
- 执行完成状态刷新恢复：通过；
- 返回执行轨迹：通过；
- 1920×1080：`scrollWidth = 1920`；
- 1440×900：`scrollWidth = 1440`；
- 1280×800：`scrollWidth = 1280`；
- 浏览器控制台 error / warning：0；
- `npm run build`：通过，412 个模块。

### 第三轮截图

截图位于：

`docs/screenshots/roadshow-simplification/`

- `01-多样化历史项目列表-1920x1080.jpg`
- `02-当前页补充资料状态-1920x1080.jpg`
- `03-精简项目设计蓝本草案-1920x1080.jpg`
- `04-唯一确认设计方向按钮-1920x1080.jpg`
- `05-六Agent连续执行轨迹-1920x1080.jpg`
- `06-六Agent全部完成状态-1920x1080.jpg`
- `07-完整成果中心与可编辑PPT入口-1920x1080.jpg`
